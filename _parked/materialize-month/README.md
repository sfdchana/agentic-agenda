# Parked: the "materialize / ship the product" month

These 20 day-files build the **visible product layer**: get axis scores into the portal,
build the 2D taste-map, the slider-search demo, deploy it to houseofsof, and post it to
LinkedIn Projects.

They were written before realizing September's planned focus is the **AI classification
layer (LangChain / LangGraph)**. This product/frontend work is real and wanted — it just
belongs *after* the classification month (a good fit once the classifier is solid — likely
slots in around the frontend/launch phase). Pull these back out and renumber into a month
when the time comes.
