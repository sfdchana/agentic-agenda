# Day 9 — LangChain: prompt templates & one source of truth

- [ ] Done

**After this you should know:** how to keep your axis rubric in one reusable place in
Python — same "single source of truth" idea as `axes.js`.

## 📖 Read (~15 min)
[LangChain — Prompt templates](https://python.langchain.com/docs/concepts/prompt_templates/).
Get: parameterized, reusable prompts instead of hardcoded strings.

## 🛠️ Build (~20 min)
Extract your rubric into a `PromptTemplate`, built from an axes definition in Python
(mirror `axes.js`). The prompt and the schema both read from one place — no drift.

## 🤖 Claude-craft (~10 min)
Use a **subagent** to search your repo for something real (e.g. every place axis scores
are written).

## 🔗 Network (if Monday)
Research: another company + one person.

## ✍️ Journal
One line: where could the Python axes and the Node axes still drift — and how to prevent it?
