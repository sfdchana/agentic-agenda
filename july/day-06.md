# Day 06 — Tool calling mechanics: one tool, full round trip

- [ ] Done

**After this you should know:** a model doesn't "call" a tool — it *emits a request*, and YOU run it.

## Do (~1 hr)
Define one tool (e.g. `get_piece(id)` reading from your archive). Send its schema, let the model request it, run it yourself, return the result, and get the model's final answer.

## 3 flashcard ideas
- Who actually executes a tool?
- The steps of one tool round-trip.
- What is a "tool schema" and why does the model need it?

_Last 2 min: a line in ../journal/ (log / question / idea). Prefix a-ha lines with `!`._
