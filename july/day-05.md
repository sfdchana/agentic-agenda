# Day 05 — Structured outputs (reliable JSON)

- [x] Done

**After this you should know:** how to make a model return JSON you can trust — validated with Pydantic.

## Do (~1 hr)
Ask the model to classify one real archive piece into your taste schema. Define a Pydantic model for the output, validate the response, and handle a parse failure gracefully. (This is the seed of Month 1's deliverable.)
Analogy: Pydantic ≈ a typed DTO + validation you'd hand-roll in Apex.

## 3 flashcard ideas
- Two ways to get structured output from a model.
- Why validate the output even when it "looks" fine?
- What do you do when the JSON is invalid?

_Last 2 min: a line in ../journal/ (log / question / idea). Prefix a-ha lines with `!`._
