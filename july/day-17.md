# Day 17 — Pure TS: async/await + fetch — call Claude from TS

- [ ] Done

**After this you should know:** you can call a model from TypeScript (the frontend/edge lane).

## Do (~1 hr)
Use `fetch` to call the Anthropic API from a `.ts` script; print the reply. Note where TS belongs (UI / edge) vs Python (data / AI).

## 3 flashcard ideas
- The `fetch` + `await` shape in TS.
- Where does TS fit in your stack vs Python?
- Parsing a JSON response in TS.

_Last 2 min: a line in ../journal/ (log / question / idea). Prefix a-ha lines with `!`._
