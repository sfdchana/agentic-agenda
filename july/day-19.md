# Day 19 — Deepen: Python depth where it bit you (refactor clean)

- [ ] Done

**After this you should know:** the everyday Python that differs from Apex — learned from the spots where the build actually made you reach for it.

## Do (~1 hr)
Pick the part of the build that felt clumsy in Python and clean it: list comprehensions, `dict` vs `Map`, f-strings, truthiness, a small class or a helper. If the classifier step is Python and the pipeline is JS, tidy the Python side into one readable module. *(This is the "Python idioms + classes" fundamental, pulled in on demand.)*
Analogy: Apex `Map` → Python `dict`; a comprehension replaces a for-loop-and-append.

## 3 flashcard ideas
- Apex `Map` → what in Python? And a `List`?
- What does a list comprehension replace?
- One Python idiom that has no clean Apex equivalent.

_Last 2 min: a line in ../journal/ (log / question / idea). Prefix a-ha lines with `!`._
