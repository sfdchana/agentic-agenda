# Day 21 — MCP: run an existing server against your agent

- [ ] Done

**After this you should know:** what MCP is and why a standard tool protocol matters.

## Do (~1 hr)
Run an existing MCP server (e.g. filesystem) and connect it to your agent; call a tool through it.

## 3 flashcard ideas
- What does MCP standardize?
- MCP server vs client.
- Why a protocol beats bespoke per-tool glue.

_Last 2 min: a line in ../journal/ (log / question / idea). Prefix a-ha lines with `!`._
