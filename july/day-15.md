# Day 15 — asyncio basics vs Apex async

- [ ] Done

**After this you should know:** Python's async model and how it differs from Queueable / Future / Batch.

## Do (~1 hr)
Convert a couple of blocking calls to `async`/`await`. Run two model calls concurrently and time it vs sequential. Map it to the Apex async you know (Queueable = ?, Future = ?).

## 3 flashcard ideas
- `async`/`await` — what problem does it solve?
- Concurrency vs parallelism.
- Apex async → Python async mapping.

_Last 2 min: a line in ../journal/ (log / question / idea). Prefix a-ha lines with `!`._
