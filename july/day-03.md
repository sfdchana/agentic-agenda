# Day 03 — First raw API call in Python (no SDK)

- [x] Done

**After this you should know:** exactly what an LLM API call is — an HTTP POST with messages, no magic.

## Do (~1 hr)
With `uv`, make a raw POST to the Anthropic Messages API using `httpx` or `requests` — no SDK. Print the JSON response.
Analogy: this is the raw callout before anyone wraps it in a helper class.
made a raw request using my api key and received JSON output with token counting and asked what a token is, received answer.
## 3 flashcard ideas
- The shape of a Messages API request (what fields?).
- Where does the API key go, and why a header?
- What's in the response JSON?

_Last 2 min: a line in ../journal/ (log / question / idea). Prefix a-ha lines with `!`._
