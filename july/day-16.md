# Day 16 — Pure TS: types, interfaces, pnpm/tsx

- [ ] Done

**After this you should know:** enough TypeScript to read it and set up a project.

## Do (~1 hr)
Set up `pnpm` + `tsx`. Write a typed function and an `interface`; run a `.ts` file with tsx.
Analogy: a TS `interface` ≈ an Apex interface + a typed DTO; TS types ≈ a compile-time governor on shape.

## 3 flashcard ideas
- TS `interface` vs `type`.
- What does `tsx` do?
- Why static types (what do they catch)?

_Last 2 min: a line in ../journal/ (log / question / idea). Prefix a-ha lines with `!`._
